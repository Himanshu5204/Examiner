{
  "name": "examiner-root",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm start",
    "client": "cd examiner && npm run dev",
    "both": "npm run start"
  },

  "devDependencies": {
    "concurrently": "^9.2.0"
  },

  "dependencies": {
    "bcrypt": "^6.0.0",
    "jsonwebtoken": "^9.0.2",
    "lucide-react": "^0.534.0",
    "react-router-dom": "^7.7.1",
    "recharts": "^3.1.0",
    "xlsx": "^0.18.5"
  }
}
